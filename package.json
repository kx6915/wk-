{
  "name": "workflow-platform-builder",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight workflow building and publishing platform with tool calls and Chinese operator labels.",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "build": "node --check src/main.js"
  },
  "devDependencies": {}
}
